@using Kitaplik2.Entities.Concrete
@model List<Publisher>

<table class="table">
    <thead>
        <tr>
            <th scope="col">#</th>
            <th scope="col">Yayıncı Adı</th>
            <th scope="col"></th>
            <th scope="col"></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <th scope="row">@item.Id</th>
                <td>@item.Name</td>
                <td><a href="/Publishers/Sil/@item.Id" class="btn btn-danger">Sil</a></td>
                <td><a href="/Publishers/Guncelle/@item.Id" class="btn btn-success">Güncelle</a></td>
            </tr>
        }
    </tbody>
</table>

<a href="/Publishers/Ekle" class="btn btn-primary">Yayıncı ekle</a>