

<table class="table">
    <thead>
        <tr>
            <th scope="col">#</th>
            <th scope="col">kitap Adı</th>
            <th scope="col"></th>
            <th scope="col"></th>
        </tr>

    </thead>

    <tbody>

        @foreach (var item in Model)
        {
            <tr>
                <th scope="row">@item.Id</th>
                <td>@item.Name</td>
                <td><a href="/Book/Sil/@item.Id" class="btn btn-danger">Sil</a></td>
                <td><a href="/Book/Guncelle/@item.Id" class="btn btn-success">Güncelle</a></td>
            </tr>
        }
    </tbody>
</table>

<a href="/Book/Ekle" class="btn btn-primary">Kategori ekle</a>